Boxer Spine 4.2 clean rebuild.
Includes json export, atlas, png atlas, and tight-cropped transparent layer PNGs.
A native .spine editor project cannot be generated directly here; import spineboy.json in Spine 4.2 and save as .spine if needed.
